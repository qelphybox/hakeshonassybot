version: '3.7'

services:
  bot:
    image: kirillbobykin/hakeshonassybot:lastest
    restart: always
    environment:
      DOTENV_CONFIG_PATH: /run/secrets/production_config
    secrets:
      - production_config

  api:
    image: kirillbobykin/hakeshonassybot:lastest
    command: npm run api
    restart: always
    ports:
      - 3000:3000
    environment:
      DOTENV_CONFIG_PATH: /run/secrets/production_config

secrets:
  production_config:
    file: .env.production
